{
  "name": "babyapp-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:articles && npm run build:sitemap",
    "build:articles": "node scripts/build-articles.mjs",
    "build:sitemap": "node scripts/generate-sitemap.mjs"
  }
}
